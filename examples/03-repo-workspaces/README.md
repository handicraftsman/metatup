# 03 - Repo Workspaces

Run:

```sh
../../metatup
```

What it shows:

- `MetaTupRepo.yaml` path repositories
- `@repo//pkg` labels in Tupfile function calls
- `spawn` into another repository
- The built-in repo workspace import model

Outputs:

- `summary.txt`
- `notice.txt`
